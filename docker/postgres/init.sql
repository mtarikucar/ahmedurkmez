-- Initialize the database
CREATE DATABASE IF NOT EXISTS ahmedurkmez_db;

-- Create a sample user (optional)
-- This will be handled by TypeORM migrations, but you can add initial data here if needed

-- Example: Insert a test user
-- INSERT INTO users (email, firstName, lastName, password, isActive, createdAt, updatedAt) 
-- VALUES ('test@example.com', 'Test', 'User', 'hashedpassword', true, NOW(), NOW());
